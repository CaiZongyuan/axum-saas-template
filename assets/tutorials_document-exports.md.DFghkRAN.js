import{_ as a,o as t,c as s,a2 as r}from"./chunks/framework.BvlN9b-o.js";const k=JSON.parse('{"title":"跟做：用后台任务导出文档","description":"","frontmatter":{},"headers":[],"relativePath":"tutorials/document-exports.md","filePath":"tutorials/document-exports.md"}'),d={name:"tutorials/document-exports.md"};function i(o,e,l,p,n,c){return t(),s("div",null,[...e[0]||(e[0]=[r(`<h1 id="跟做-用后台任务导出文档" tabindex="-1">跟做：用后台任务导出文档 <a class="header-anchor" href="#跟做-用后台任务导出文档" aria-label="Permalink to &quot;跟做：用后台任务导出文档&quot;">​</a></h1><p>登录后打开已保存文档，在“导出文档”点击“导出当前文档”。页面立即显示等待处理，Worker 生成后出现“下载 ZIP”。记录按最新优先显示，新申请会回到第一页并显示进度；刷新页面仍能找到记录。Reader 也可以申请自己有权读取的文档。ZIP 包含 <code>document.md</code> 和附件目录，适合离线保存单篇文档。</p><p>这条路径把一个耗时动作从 HTTP 请求拆开：API 负责保存用户的请求，Worker 负责完成它。任务留在 PostgreSQL，停止 Worker 不会把待处理记录丢在内存里；本章不需要 RabbitMQ。</p><h2 id="_1-启动并实际操作" tabindex="-1">1. 启动并实际操作 <a class="header-anchor" href="#_1-启动并实际操作" aria-label="Permalink to &quot;1. 启动并实际操作&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">just</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span></span></code></pre></div><p>开发入口现在启动 PostgreSQL/RustFS，运行迁移和存储初始化，再启动 API、Worker 与 Web。Rust 源码修改会重新启动 API 和 Worker；Web 使用 HMR。Worker 默认健康端口为 <code>127.0.0.1:3001</code>。</p><p>创建一篇 Markdown，上传一个文本附件，然后申请导出。下载 ZIP，确认正文和附件都在。想观察等待状态时，可以单独使用下面命令启动 Worker；只启动 API/Web 时，请求依然入队。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">just</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> worker</span></span></code></pre></div><p><code>just worker</code> 加载同一份 <code>.env.example</code> / <code>.env</code> 与进程环境，启动一个真实 Worker。不要与 <code>just dev</code> 的 Worker 同时占用默认健康端口；第二个进程使用独立 <code>WORKER_BIND</code>。</p><p>Worker 的 <code>/health/live</code> 表示进程存活，<code>/health/ready</code> 同时检查循环已启动与完整迁移历史。<code>Ctrl+C</code> 先停止领取任务，再给当前任务有限的完成时间；数据库和对象存储卷保留。</p><h2 id="_2-申请时保存快照" tabindex="-1">2. 申请时保存快照 <a class="header-anchor" href="#_2-申请时保存快照" aria-label="Permalink to &quot;2. 申请时保存快照&quot;">​</a></h2><p><a href="https://github.com/CaiZongyuan/axum-saas-template/blob/14acdb0c78d3ec91abf486504116982dcd373c9f/crates/app/src/modules/knowledge/exports/mod.rs" target="_blank" rel="noreferrer">导出 API</a>验证当前文档阅读权，在同一事务内保存：</p><ul><li>Export ID 与请求者；</li><li>文档版本、标题、Markdown、ready 附件清单；</li><li>原始凭据的稳定引用；</li><li>PostgreSQL Job 与 Audit。</li></ul><p>Job payload 只保存 <code>export_id</code>，不复制正文或存储 URL。原始 Session ID 用于随后重新验证，密码、Cookie secret 和哈希不会进入任务 payload。</p><p>Idempotency-Key 标识一次导出请求。响应丢失时用相同 key 重试，会取得同一个 Export；其间编辑文档也不会改写这份快照。成功申请后的下一次主动点击会创建新请求，用于导出较新的版本。审计或其他数据库写入失败时，Export、快照、Job 和幂等记录一起回滚。</p><h2 id="_3-core-jobs-与知识库-handler" tabindex="-1">3. Core Jobs 与知识库 Handler <a class="header-anchor" href="#_3-core-jobs-与知识库-handler" aria-label="Permalink to &quot;3. Core Jobs 与知识库 Handler&quot;">​</a></h2><p><a href="https://github.com/CaiZongyuan/axum-saas-template/blob/14acdb0c78d3ec91abf486504116982dcd373c9f/crates/app/src/modules/jobs/mod.rs" target="_blank" rel="noreferrer">Core Jobs</a>用短事务的 <code>FOR UPDATE SKIP LOCKED</code> 领取任务，更新尝试次数、执行者、lease_token 和到期时间后提交。后续网络 I/O 与 ZIP 生成不占着领取事务。</p><p><a href="https://github.com/CaiZongyuan/axum-saas-template/blob/14acdb0c78d3ec91abf486504116982dcd373c9f/crates/app/src/modules/jobs/worker.rs" target="_blank" rel="noreferrer">Worker</a>只领取已注册的任务类型，处理期间独立推进续租。续租等行锁时业务仍能继续；续租失败会先取消 Handler、释放其事务，再记录失败，避免自己等待自己持有的锁。完成提交必须仍匹配 token 且租约未过期。知识库 <a href="https://github.com/CaiZongyuan/axum-saas-template/blob/14acdb0c78d3ec91abf486504116982dcd373c9f/crates/app/src/modules/knowledge/exports/worker.rs" target="_blank" rel="noreferrer">Handler</a>负责自己的快照、权限和业务结果；Core 不查询知识库表。</p><p>Worker 在开始处理、创建候选和最终发布时检查请求者与原始凭据的有效性、源文档和附件。申请后退出该 Session、停用成员、撤销 Grant 或丢失输入，都不能生成可下载的成功结果。一个已经完成的导出在用户用新 Session 登录后仍可按当前权限访问。</p><p>基础队列已区分临时错误与永久错误，包含有预算的退避和过期租约领取。继续跟做<a href="./job-recovery">任务恢复与管理员重试</a>，验证崩溃、旧执行者、尝试历史和管理员重试。</p><h2 id="_4-有限内存与安全-zip" tabindex="-1">4. 有限内存与安全 ZIP <a class="header-anchor" href="#_4-有限内存与安全-zip" aria-label="Permalink to &quot;4. 有限内存与安全 ZIP&quot;">​</a></h2><p><a href="https://github.com/CaiZongyuan/axum-saas-template/blob/14acdb0c78d3ec91abf486504116982dcd373c9f/crates/app/src/modules/knowledge/exports/archive.rs" target="_blank" rel="noreferrer">ZIP 实现</a>不把完整 ZIP 或全部附件放进内存：</p><ol><li>顺序读取一个 S3 对象流，落入权限为 <code>0700</code> 的临时目录，同时计数并核对 SHA-256；</li><li>使用阻塞线程从临时输入文件分块写 ZIP，限制输出文件实际长度，包括 ZIP 头和中央目录；</li><li>完成并关闭 ZIP 写入后，重新读取最终文件计算校验和；</li><li>用文件 ByteStream 条件 PUT 上传。</li></ol><p>ZIP 的 Stored 模式不压缩，便于理解并限制 CPU 开销。内部路径为 <code>document.md</code> 与 <code>attachments/&lt;File ID&gt;.&lt;安全扩展名&gt;</code>，用户文件名不会变成目录路径，同名附件也不会冲突。</p><p>Markdown 先解析，仅改写 Link/Image 中属于快照的 <code>attachment:&lt;UUID&gt;</code>，转成 ZIP 内的相对路径；代码和普通文字中的相同字符串仍是文字。出现附件引用时，重新序列化可能规范化空白、转义和引用式链接；导出保证快照内容的语义，不承诺保留原 Markdown 的每个格式字节。未知附件引用保留原目标，不偷偷导入其他文件，也不抓取外部 URL。</p><p>同一进程最多一个导出占用打包资源。总 deadline 覆盖下载、ZIP 与发布；阻塞代码按块检查取消。<code>spawn_blocking</code> 已经开始后不能强行 abort，因此线程持有临时目录和并发许可，直到它实际退出。普通成功/失败会清理本地文件；SIGKILL 或文件系统故障可能留下临时目录，不能把 Drop 当作崩溃清理保证。临时磁盘不参与备份，失败任务可从数据库快照重建。</p><p>主来源与确切库版本见<a href="https://github.com/CaiZongyuan/axum-saas-template/blob/14acdb0c78d3ec91abf486504116982dcd373c9f/docs/research/document-export-streaming.md" target="_blank" rel="noreferrer">流式处理研究</a>。</p><h2 id="_5-对象写入与数据库发布" tabindex="-1">5. 对象写入与数据库发布 <a class="header-anchor" href="#_5-对象写入与数据库发布" aria-label="Permalink to &quot;5. 对象写入与数据库发布&quot;">​</a></h2><p>生成的 ZIP 在写入 RustFS 前，先通过 <a href="https://github.com/CaiZongyuan/axum-saas-template/blob/14acdb0c78d3ec91abf486504116982dcd373c9f/crates/app/src/modules/files/mod.rs" target="_blank" rel="noreferrer">Core Files</a>登记 File 和唯一候选位置。S3 条件 PUT 使用 <code>If-None-Match: *</code>，已存在的候选不能覆盖；上传后核对对象大小、类型和上传标识。</p><p>最后重新鉴权、检查有效租约，在同一事务采用 ready 文件、关联 Export、写 Audit 并将 Job 标为 succeeded。超时不证明远端写入失败；未采用的候选和 pending File 仍有持久元数据，后续对象清理章节据此回收。</p><p>导出和快照默认保存 24 小时；到期查询显示 expired，不再提供新链接。下载只允许请求者或管理员，而且当前访问者和原请求者都必须仍可读取源文档。现有短期链接沿用附件教程声明的 TTL 边界；导出不会变成永久公开地址。物理删除与过期对象清理在删除章节实现。</p><h2 id="_6-配置与验证" tabindex="-1">6. 配置与验证 <a class="header-anchor" href="#_6-配置与验证" aria-label="Permalink to &quot;6. 配置与验证&quot;">​</a></h2><p>实际配置来自<a href="./../reference/config">生成配置参考</a>：</p><table tabindex="0"><thead><tr><th>配置</th><th>默认值</th><th>用途</th></tr></thead><tbody><tr><td><code>EXPORT_MAX_ATTACHMENTS</code></td><td>100</td><td>限制条目数</td></tr><tr><td><code>EXPORT_MAX_INPUT_BYTES</code></td><td>256 MiB</td><td>正文与附件总量</td></tr><tr><td><code>EXPORT_MAX_OUTPUT_BYTES</code></td><td>272 MiB</td><td>包含目录和头部的 ZIP 上限</td></tr><tr><td><code>EXPORT_TIMEOUT_SECS</code></td><td>120 秒</td><td>一次执行的总预算</td></tr><tr><td><code>EXPORT_RETENTION_SECS</code></td><td>24 小时</td><td>导出与快照有效期</td></tr><tr><td><code>JOB_LEASE_SECS / JOB_HEARTBEAT_SECS</code></td><td>60 / 20 秒</td><td>任务租约及续租</td></tr><tr><td><code>JOB_SHUTDOWN_SECS</code></td><td>10 秒</td><td>Worker 关停等待</td></tr></tbody></table><p>临时磁盘峰值约为输入加输出上限，每个活动导出默认约 528 MiB。磁盘不足会使该次尝试失败；这些上限与普通附件的 20 MiB 单文件上限分开。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> scripts/test-backend.mjs</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --test</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exports</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --test</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> jobs</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> scripts/test-storage.mjs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> generated_files</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vitest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apps/web/src/exports.test.tsx</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">just</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> check</span></span></code></pre></div><p>HTTP/公开任务测试验证发布跨越续租、续租超时后的事务释放、超过一页历史的新任务可见性、幂等快照、真实 ZIP/附件字节、Session 失效、审计回滚、权限与到期、缺失对象和大小限制。存储适配测试验证多块传输与条件写入；View 测试验证进度、请求重试、失败、过期和下载被拒绝。</p><p>完成这条关键旅程后使用真实浏览器验收：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> scripts/e2e.mjs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/e2e/exports.spec.ts</span></span></code></pre></div><p>浏览器上传附件、申请导出，真实 Worker 生成 ZIP，再由独立 JS ZIP 库解压校验正文与附件，最后刷新确认结果可再次访问。日常开发继续使用快速 HTTP/View 反馈。</p><p>实现自己的业务时，仿照“在业务事务中入队 → 注册 Handler → 在有效租约内提交结果”，不用复制队列实现。<a href="https://github.com/CaiZongyuan/axum-saas-template/blob/14acdb0c78d3ec91abf486504116982dcd373c9f/examples/knowledge-base/manifest.json" target="_blank" rel="noreferrer">示例所有权清单</a>记录知识库 Handler、Views、迁移、测试、配置与教程；Core Jobs、Files、Worker 和 S3 适配器保留。</p><hr><p>源码版本：<code>14acdb0c78d3</code> · <a href="https://github.com/CaiZongyuan/axum-saas-template/blob/14acdb0c78d3ec91abf486504116982dcd373c9f/docs/tutorials/10-document-exports.md" target="_blank" rel="noreferrer">本页 Markdown</a></p>`,43)])])}const b=a(d,[["render",i]]);export{k as __pageData,b as default};
